# 2026-09-03

## 工作区熟悉（首次）
通读 `D:\claude code\IRIS` 全貌，建立项目认知底图。要点记录：

- **项目性质**：InterSystems IRIS for Health 2021.1 之上的医院信息系统（HIS）二次开发工作区，非单一应用仓库，而是"多需求项目并行 + 规范沉淀"的工作台。
- **核心规范文件**：`CLAUDE.md`（17KB），含全局约束（原文件只读、产出进"优化中"、CLS 必须源码格式、15 条 IRIS 编译陷阱、编码 BOM 规则、CLS 自测 test() 约定）。**任何开发动作前必须先遵守它**。
- **目录职责**：
  - `HISUI-新建/` — 22 个新建需求项目，每个含需求文档 + `01代码实现/`（.cls/.csp/.js）
  - `HISUI-优化/` — `00原文件/`(只读) + `01优化中/`(产出) + `02更新文件/` + `03代码分析/`
  - `HISUI-工具表/` — Global 属性字典（XML + `-props.csv`，csv 的 `propertyPiece` 是字段位置权威来源）
  - `references/` — 9 份领域参考（数据模型/Global速查/UI规范/避坑指南/可复用组件库 等）
  - `AI需求/` — 13 份已提交需求 + 模块清单 + 需求总表
- **近期活跃**（近 7 天）：HQMS数据导入、国家医保药品查询、追溯码台账/拆零查询、基数药维护、通用组件库
- **自动化侧**：`.claude/skills/` 18 个 skill，其中 `imedical-dailyreport` / `imedical-weeklyreport` / `imedical-project-weekly` 用 Playwright 自动填报东华医为协同网日报周报；`.claude/scheduled_tasks.json` 有每月 1-10 号周报补交提醒。
- **git**：仅 9 次提交，最后一次 `25b694e fix: 修正后置条件表达式空格导致的编译错误(#1054)`；当前工作区有较多未提交改动（主要在 .claude/skills 与 CLAUDE.md）。

## 约定备忘
- 涉及 Global 名/索引/Piece 位置不确定时，先查 `HISUI-工具表/` 的 `-props.csv`，不得凭记忆猜测。
