# 2026-09-05 工作记录

## 通用数据采集字典 ↔ HIS 字典对照界面（HISUI） — 初版（当时名「三医数据采集字典」，2026-09-06 改名）

基于 `HISUI-新建/20260907第三方平台自定对照界面/4.2通用数据采集标准(值域代码分册)_v1.6.docx`（原名 `4.2三医数据采集标准...`，305 个值域、12841 条目）完成对照维护界面。

交付物（`HISUI-新建/20260907第三方平台自定对照界面/`，2026-09-06 起文件名随系统名一并改名）：
- `01代码实现/web.YZSY.DHCSYDictMap.cls`：后端，存储 `^YZSY("SYDictMap",...)`，含平台/字典表注册/值域/条目/对照关系/反查、增删改查、探测、导入导出、初始化（InitDomain 注入 305 值域定义）。
- `01代码实现/sanyidictmap.csp` + `sanyidictmap.js`：三 Tab 界面（对照维护 / 关系查询 / 字典导入维护）。左侧采集字典，右侧 HIS 字典多选对照；未绑定字典时切换手工录入面板。
- `通用值域导入.csv` / `通用值域明细.csv` / `通用值域清单.csv`（原 `三医值域导入.csv` 等）：从 docx 解析出的种子数据。
- `01代码实现/预览.html`：可离线打开的预览（内置 jQuery + datagrid 垫片 + mock 后端），`node _smoke.js` 端到端冒烟测试 18/18 通过。

关键约定（已验证）：
- JS 调后端统一用 `api('MethodName', params, cb)` → `$m({ClassName,MethodName}, cb)`；前端 23 个 MethodName 与 CLS 方法名一一对应。
- 生成预览用 `_make_assets.py`（重建需 `python _make_assets.py`）；重建 CLS 值域用 `_gen_initdomain.py`。冒烟测试 `_smoke.js`（依赖 `_tmp/jquery-1.11.3.min.js` + jsdom）。
- 修复点：`page()` 分页函数原先倒序输出（会导致所有列表倒序），已改为正向；mock 种子对照原写死 item 码 `'1'`，已改为按真实首条 item 码动态挂接。

HIS 字典 Global 注册示例：CTLOC(科室)、CTNAT(民族)、TARI(收费项目)、MRC(ICD)、DHCPatType(患者类型) 等，piece 字段待真实环境 `ProbeHisDict` 探测确认后启用。
