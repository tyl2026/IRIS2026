需求1：[AI需求]通用采集字典对照 — 采集平台维护
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 维护第三方采集标准时，需要先登记采集平台（平台代码、名称、版本），目前没有地方登记，只能临时找工程师手工处理。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 提供采集平台的新增、修改、删除功能，可维护平台代码、名称、版本、启用状态和备注。

需求2：[AI需求]通用采集字典对照 — SQL 方式注册 HIS 字典表
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 做对照前需要把 HIS 已有的字典表登记进来，目前没有登记入口。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 支持填写 SQL 查询语句注册 HIS 字典表，保存后可维护启用状态和备注。

需求3：[AI需求]通用采集字典对照 — 枚举方式维护 HIS 字典
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 部分 HIS 没有现成字典表，需要手工逐行列出字典代码和名称，目前没有维护入口。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 支持按枚举方式逐行维护字典代码和名称，可批量粘贴、逐条增删改。

需求4：[AI需求]通用采集字典对照 — 字典取数探测
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 注册的 HIS 字典不确定能否取到数据，直接使用容易导致对照为空。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 提供“探测”按钮，点击后展示字典取数样例，确认取数正常后再用于对照。

需求5：[AI需求]通用采集字典对照 — 值域绑定 HIS 字典
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 采集值域需要先绑定对应的 HIS 字典才能开展对照，目前没有绑定操作。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 支持为采集值域绑定、更换或解除 HIS 字典，未绑定前明确提示先维护 HIS 字典。

需求6：[AI需求]通用采集字典对照 — 勾选保存对照关系
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 把采集条目与 HIS 字典条目逐个建立对应关系，目前靠手工逐条录入，效率低易出错。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 在 HIS 字典列表中勾选条目后点击“保存对照”，一次性建立与当前采集条目的对照关系。

需求7：[AI需求]通用采集字典对照 — 防重复对照锁定
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 同一个 HIS 代码可能被多个采集条目重复对照，导致数据混乱。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 同一值域内一个 HIS 代码只能对照一个采集条目，已被其他条目对照的代码锁定置灰、浅绿标识，不允许重复对照。

需求8：[AI需求]通用采集字典对照 — 对照关系修改与清除
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 已建立的对照关系需要修改描述、状态、备注，也需要删除或清空，目前缺少操作入口。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 支持对照关系的修改、删除、单条目清空和批量清除，并可一键按当前 HIS 字典刷新描述。

需求9：[AI需求]通用采集字典对照 — 对照关系查询
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 需要按平台、值域、关键字等条件查看已建立的对照关系，目前没有查询页面。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 提供对照关系查询页，支持按平台、值域、关键字、状态筛选，结果列表翻页展示。

需求10：[AI需求]通用采集字典对照 — 批量启停与删除
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 停用或清理大量对照关系时逐条操作太慢。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 查询结果支持勾选后批量启用、批量停用、批量删除，并可导出 CSV 文件。

需求11：[AI需求]通用采集字典对照 — 对照覆盖查询
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 想知道某个 HIS 字典里哪些代码已对照、哪些未对照，目前没有直观查看方式。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 提供覆盖查询页，按平台和 HIS 字典查看每个代码的对照状态，未对照红色标识，并显示覆盖率统计。

需求12：[AI需求]通用采集字典对照 — 采集值域批量导入
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 采集标准值域和条目数量大，手工录入不现实。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 支持上传 CSV 文件或按服务器路径批量导入值域和条目，完成后显示成功、失败数量。

需求13：[AI需求]通用采集字典对照 — 维护界面四块并列展示
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 维护界面四个区域上下排列，需要来回滚动查看，操作不便。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 值域、条目、HIS 字典、当前对照四块并列展示，窗口缩放时高度自适应不出现空白。

需求14：[AI需求]通用采集字典对照 — 列表翻页与每页条数独立设置
模块：标准管理（国家）
产品线：医院服务总线
产品组：数据中心基础平台
现状： 数据较多时列表一次性全部显示，查找和操作不便。安全组：信息科，科室：信息科，菜单：通用采集字典对照。
需求： 四块列表底部分别提供翻页条，可单独设置每页显示条数，互不影响。
